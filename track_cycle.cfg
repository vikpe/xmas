alias t_init "set _tnum 0; set _tcount 8"
alias t_next "inc _tnum; if ($_tnum > $_tcount) then set _tnum 1;  t_track"
alias t_prev "inc _tnum -1; if ($_tnum < 1) then set _tnum $_tcount;  t_track"
alias t_track "track #$_tnum"

t_init

bind mouse1 t_next
bind mouse2 t_prev